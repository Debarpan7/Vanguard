import { SectionPlaceholder } from "@/components/section-placeholder";

export default function MetricsPage() {
  return (
    <SectionPlaceholder
      title="Metrics"
      description="Current financial metrics — AUM, number of clients, revenue, expenses, RoE — each with a 5-year trend from the fact base."
    />
  );
}
