import { SectionPlaceholder } from "@/components/section-placeholder";

export default function BenchmarkingPage() {
  return (
    <SectionPlaceholder
      title="Benchmarking"
      description="Vanguard's metrics compared against the peer set over the 5 years, with membership rules and caveats."
    />
  );
}
