import { SectionPlaceholder } from "@/components/section-placeholder";

export default function RoeTreePage() {
  return (
    <SectionPlaceholder
      title="RoE tree"
      description="Return on equity decomposed into its drivers across the 5 years, with each node traceable to published numbers."
    />
  );
}
