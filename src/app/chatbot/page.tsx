import { SectionPlaceholder } from "@/components/section-placeholder";

export default function ChatbotPage() {
  return (
    <SectionPlaceholder
      title="Chatbot"
      description="Ask questions about Vanguard's metrics, benchmarking, and RoE analysis — answers grounded in the fact base with sources."
    />
  );
}
